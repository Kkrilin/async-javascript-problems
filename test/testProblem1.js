const myModule = require('../problem1');
const callBackFs = myModule.callBackFs;
const usingPromise = myModule.usingPromise;
const usingAsyncAwait = myModule.usingAsyncAwait;

callBackFs('callback');

// usingPromise('promiseChain');

// usingAsyncAwait('asyncAwait');
